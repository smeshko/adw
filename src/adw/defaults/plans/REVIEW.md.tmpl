# Review Summary — {slug}

**Rounds:** {N}
**Fix commits:** {first-fix-sha}..{last-fix-sha}

## Rounds

| Round | Findings | Fixed | Deferred | Rejected |
|-------|----------|-------|----------|----------|
| 1     |          |       |          |          |
| 2     |          |       |          |          |
{# Add a round 3 row only if a third round ran. }

## Fixes

<!-- One bullet per fix commit, grouped by round. Reference the round/finding number. -->

### Round 1
- `{sha}` — {one-line summary} (round-1 #{n})

### Round 2
- `{sha}` — {one-line summary} (round-2 #{n})

## Deferred

<!-- Findings worth doing later but out of scope here. Each becomes a follow-up suggestion in the PR body. -->

- (round-{R} #{n}) {one-line summary} — {rationale}

## Rejected

<!-- Findings we pushed back on. Surfaces what Codex flagged and why we said no, so PR reviewers see the reasoning. -->

- (round-{R} #{n}) {one-line summary} — {rationale, referencing the Decision in PLAN.md if applicable}
