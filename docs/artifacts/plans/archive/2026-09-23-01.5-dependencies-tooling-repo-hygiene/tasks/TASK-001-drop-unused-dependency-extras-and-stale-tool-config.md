# TASK-001: Drop unused dependency extras and stale tool config

Depends on: None
Suggested commit: `chore(deps): drop unused extras and stale tool config`

## Goal

`pyproject.toml` depends on plain `typer` and `uvicorn` and carries no config for missing modules, files or markers. `uv.lock` loses the four `uvicorn[standard]` packages. Nothing else changes.

## Files

- `pyproject.toml`:
  - `[project].dependencies`: `"typer[all]>=0.21.0"` → `"typer>=0.21.0"`, `"uvicorn[standard]>=0.32.0"` → `"uvicorn>=0.32.0"`.
  - `[[tool.mypy.overrides]]`: the `module` list keeps only `"markdown"`. Delete `httpx`, `httpx.*`, `playwright` and `playwright.*`.
  - `[tool.ruff.lint.per-file-ignores]`: delete the `"src/adw/models/evidence.py" = ["E501"]` line.
  - `[tool.pytest.ini_options]`: delete `python_files`, `python_classes`, `python_functions` and the whole `markers` list. Keep `testpaths`, `asyncio_mode` and `addopts`, including `--strict-markers`.
- `uv.lock`: regenerated by `uv lock`.

## Acceptance

- [ ] `git diff uv.lock` only removes lines. It drops `httptools`, `uvloop`, `watchfiles`, `websockets` and their exclusive transitive deps, plus the `extra = [...]` markers on the `typer`/`uvicorn` requirement lines. No other package changes version.
- [ ] `grep -cE '^name = "(httptools|uvloop|watchfiles|websockets)"' uv.lock` prints `0`.
- [ ] `uv sync --locked` passes.
- [ ] `uv run mypy src/adw` passes without the `httpx` override.
- [ ] `uv run pytest --collect-only -q | tail -1` prints the same count before and after the edit.
- [ ] `adw dashboard web --reload --no-browser --port 8199` starts with uvicorn's `StatReload` line, and `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8199/` prints `200`.
- [ ] `scripts/preflight.sh` passes.

Evidence:
- the `uv.lock` diff stat and the `grep -c` output
- both collect-only lines
- the dashboard startup log line naming `StatReload`, plus the `200`

## Steps

- [ ] Record the baseline: `uv run pytest --collect-only -q | tail -1`.
- [ ] Edit `pyproject.toml` as listed under Files.
- [ ] `uv lock` (no `--upgrade`), then `git diff --stat uv.lock` and `git diff uv.lock | grep '^+' | grep -v '^+++'`. Expect no added package lines.
- [ ] `uv sync --locked`, then `uv run mypy src/adw`.
- [ ] Re-run the collect-only line and compare it to the baseline.
- [ ] Start `uv run adw dashboard web --reload --no-browser --port 8199` in the background with output to a scratchpad log. `curl` the root, `grep -i statreload` the log, then kill the server's process group.
- [ ] `scripts/preflight.sh`.

## Notes

- `--strict-markers` stays in `addopts`. With the `markers` list gone, any future `@pytest.mark.slow` fails at collection, which is what we want.
- The dashboard server reads the real `~/.adw` index. Only `GET /`, and stop the server afterwards.
