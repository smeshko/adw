# ADW - Agentic Development Workflow SDK

A command-line SDK for orchestrating agentic development workflows.

## Installation

Install the `adw` CLI from a checkout:

```bash
uv tool install --editable .
```

For development:

```bash
uv sync
```

## Usage

```bash
adw --help
adw --version
```
